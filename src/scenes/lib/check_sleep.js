//checks whether the current player has won the game
module.exports = function(scene){
	
	

	if(
		((scene.player.stress>= 50 || scene.player.relaxation <= 0 || scene.player.happiness <= 0) 
	) ){
		
		
	}
}